<!DOCTYPE html>
<html lang="en">
  <head>
    <title>FortStats</title>
    <meta charset="utf-8" />
    <link rel="stylesheet" href="/stylehseets/style.css">
  </head>
  <body>
    <header>
      <h1>FortStats</h1>
      <nav>
        <ul>
          <li>
            <a href="/">Home</a>
          </li>
          <li>
            <a href="/player/stats/combined/<%= @current_season.first[:number] %>">My Stats</a>
          </li>
          <li>
            <a href="">Players</a>
          </li>
          <li>
            <a href="">Leaderboard</a>
          </li>
        </ul>
      </nav>
    </header>
    <section>
      <% if session[:success] %>
        <div class="flash success">
          <p><%= session.delete(:success) %></p>
        </div>
      <% end %>

      <% if session[:error] %>
        <div class="flash error">
          <p><%= session.delete(:error) %></p>
        </div>
      <% end %>
    </section>
    <main>
      <%== yield %>
    </main>
  </body>
</html>